# DiabetesDetectionAPPusingML


Install the follwing library by "pip install streamlit scikit-learn pil"


Run the App through command prompt, by following instructions:


streamlit run "C:\<Directory>\WebApp.py"
